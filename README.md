# Hi, I'm Tevvez (HallofFameClient) 👋

C# / .NET developer from Germany. I build launchers, UI frameworks and Windows tooling — mostly open source.

## 🔥 What I do

- **Desktop launchers** — e.g. `SSOLauncher`: unofficial Star Stable Online launcher in .NET 8 + WinForms + Native AOT, single-file exe, multi-account, auto-login, file verification
- **UI frameworks** — `TevvezImGui`: Dear ImGui menu framework for .NET (ImGui.NET + Silk.NET OpenGL), standalone window + transparent overlay, no cimgui.dll. Sibling project `TevvezUI` in WinForms/GDI+
- **Windows internals & tooling** — `Memory`: 32/64-bit process-memory helper fork (Read/Write/AoB-scan/Freeze) for learning and debugging your own apps, `MMInject`: NativeAOT manual-map research demo
- **Reverse engineering (learning-focused)** — static + dynamic analysis of my own apps for debugging and interoperability: dnSpy / ILSpy, x64dbg, Process Monitor, Wireshark for own traffic, PE headers, PEB/LDR loader internals
- **Focus:** C#, .NET 8, WinForms, ImGui, NativeAOT single-file publishes, clean standalone exes

## 🔬 Reverse engineering interests

- `.NET` decompilation & debugging (my own builds, crash triage via `last-error.txt` style logs)
- Windows loader internals: PE mapping, `LDR_DATA_TABLE_ENTRY`, PEB lists, TLS, CFG — for understanding, not for bypassing protections
- API research with public docs + local observation (own launcher flows, config endpoints)
- Strictly no anti-cheat / DRM bypass work published here

## 📌 Pinned (ToS-safe picks)

1. `SSOLauncher` — unofficial SSO launcher, open source
2. `TevvezImGui` — custom ImGui UI without cimgui.dll (+ `TevvezOverlay`)
3. `Memory` — learning-focused memory helper library

> Tip: pin only projects that respect game ToS. Bypass / cheat-client repos get flagged and risk account strikes — keep them unlisted or private.

## 🛠️ Stack

`C#` ` .NET 8` `WinForms` `ImGui.NET` `Silk.NET` `NativeAOT` `Win32 API` `dnSpy/ILSpy` `x64dbg` `PE internals`

## 📫 Contact

- Telegram: `t-me/+Ba98xQhu8MIzMjBi`
- GitHub: `HallofFameClient`

## ⚖️ Ground rules

- Unofficial projects are marked as such (e.g. not affiliated with Star Stable Entertainment AB)
- No anti-cheat bypasses, no cheat-client distribution here — only learning-safe tooling and UI work
- RE only on own software / with permission, and respecting each game's Terms of Service
- Use everything at your own risk
